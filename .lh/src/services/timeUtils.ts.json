{
    "sourceFile": "src/services/timeUtils.ts",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 1,
            "patches": [
                {
                    "date": 1783673969440,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1783674451922,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,44 +1,17 @@\n /**\n- * Преобразует список времён приёма [\"09:00\", \"21:00\"] в cron-выражение \"0 9,21 * * *\"\n- * Работает только для времён, кратных минутам (без секунд).\n+ * Преобразует одно время приёма \"ЧЧ:ММ\" в cron-выражение \"m h * * *\".\n+ * Каждое время планируется отдельной повторяющейся задачей BullMQ,\n+ * поэтому напоминания с разными минутами (например 09:00 и 21:30) не теряются.\n  */\n-export function timesToCron(times: string[]): string {\n-  const hours = new Set<number>();\n-  const minutesByHour: Record<number, number[]> = {};\n-\n-  for (const t of times) {\n-    const [hStr, mStr] = t.split(\":\");\n-    const h = Number(hStr);\n-    const m = Number(mStr ?? \"0\");\n-    if (Number.isNaN(h) || Number.isNaN(m) || h < 0 || h > 23 || m < 0 || m > 59) {\n-      throw new Error(`Некорректное время: ${t}. Используй формат ЧЧ:ММ, например 09:00`);\n-    }\n-    hours.add(h);\n-    minutesByHour[h] = minutesByHour[h] || [];\n-    minutesByHour[h].push(m);\n+export function timeToCron(time: string): string {\n+  const [hStr, mStr] = time.split(\":\");\n+  const h = Number(hStr);\n+  const m = Number(mStr ?? \"0\");\n+  if (Number.isNaN(h) || Number.isNaN(m) || h < 0 || h > 23 || m < 0 || m > 59) {\n+    throw new Error(`Некорректное время: ${time}. Используй формат ЧЧ:ММ, например 09:00`);\n   }\n-\n-  // Упрощение: берём общий набор минут (обычно у всех приёмов минута одинаковая, например :00)\n-  const allMinutes = new Set<number>();\n-  Object.values(minutesByHour).forEach((arr) => arr.forEach((m) => allMinutes.add(m)));\n-\n-  if (allMinutes.size === 1) {\n-    const minute = [...allMinutes][0];\n-    const hourList = [...hours].sort((a, b) => a - b).join(\",\");\n-    return `${minute} ${hourList} * * *`;\n-  }\n-\n-  // Если минуты разные — строим через запятую по каждому часу отдельно не получится одним cron,\n-  // поэтому в этом случае берём минуту 0 по умолчанию (упрощение для MVP)\n-  // [DEBUG] Путь потери данных: разные минуты принудительно сворачиваются в :00\n-  console.log(\n-    `[DEBUG timesToCron] DATA LOSS: input=${JSON.stringify(times)} minutes=${JSON.stringify(\n-      [...allMinutes]\n-    )} -> принудительно minute=0 (напоминания с другими минутами будут потеряны)`\n-  );\n-  const hourList = [...hours].sort((a, b) => a - b).join(\",\");\n-  return `0 ${hourList} * * *`;\n+  return `${m} ${h} * * *`;\n }\n \n export function isValidTimeFormat(value: string): boolean {\n   return /^([01]\\d|2[0-3]):([0-5]\\d)$/.test(value.trim());\n"
                }
            ],
            "date": 1783673969440,
            "name": "Commit-0",
            "content": "/**\n * Преобразует список времён приёма [\"09:00\", \"21:00\"] в cron-выражение \"0 9,21 * * *\"\n * Работает только для времён, кратных минутам (без секунд).\n */\nexport function timesToCron(times: string[]): string {\n  const hours = new Set<number>();\n  const minutesByHour: Record<number, number[]> = {};\n\n  for (const t of times) {\n    const [hStr, mStr] = t.split(\":\");\n    const h = Number(hStr);\n    const m = Number(mStr ?? \"0\");\n    if (Number.isNaN(h) || Number.isNaN(m) || h < 0 || h > 23 || m < 0 || m > 59) {\n      throw new Error(`Некорректное время: ${t}. Используй формат ЧЧ:ММ, например 09:00`);\n    }\n    hours.add(h);\n    minutesByHour[h] = minutesByHour[h] || [];\n    minutesByHour[h].push(m);\n  }\n\n  // Упрощение: берём общий набор минут (обычно у всех приёмов минута одинаковая, например :00)\n  const allMinutes = new Set<number>();\n  Object.values(minutesByHour).forEach((arr) => arr.forEach((m) => allMinutes.add(m)));\n\n  if (allMinutes.size === 1) {\n    const minute = [...allMinutes][0];\n    const hourList = [...hours].sort((a, b) => a - b).join(\",\");\n    return `${minute} ${hourList} * * *`;\n  }\n\n  // Если минуты разные — строим через запятую по каждому часу отдельно не получится одним cron,\n  // поэтому в этом случае берём минуту 0 по умолчанию (упрощение для MVP)\n  // [DEBUG] Путь потери данных: разные минуты принудительно сворачиваются в :00\n  console.log(\n    `[DEBUG timesToCron] DATA LOSS: input=${JSON.stringify(times)} minutes=${JSON.stringify(\n      [...allMinutes]\n    )} -> принудительно minute=0 (напоминания с другими минутами будут потеряны)`\n  );\n  const hourList = [...hours].sort((a, b) => a - b).join(\",\");\n  return `0 ${hourList} * * *`;\n}\n\nexport function isValidTimeFormat(value: string): boolean {\n  return /^([01]\\d|2[0-3]):([0-5]\\d)$/.test(value.trim());\n}\n"
        }
    ]
}